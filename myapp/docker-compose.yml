services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    restart: always

  frontend:
    build: .
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: always
